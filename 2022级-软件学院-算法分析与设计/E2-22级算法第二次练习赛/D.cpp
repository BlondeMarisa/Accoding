#include <bits/stdc++.h>
#define eps 1e-10
#define ll long long
#define PI acos(-1)
#define MOD 100000007
#define N 100010
using namespace std;
int a[N];
ll mypow(ll x, ll n) // x^n
{
  ll ans = 1;
  while (n--)
  {
    ans *= x;
  }
  return ans;
}
int ans[15];
int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  a[1]=45;
  for(int i=1;i<=)
  cout<<a[2];
  return 0;
}
